import React from "react";

const Header: React.FC = () => {
    return (
        <div id="header" className="h-[120px] w-[100%] absolute top-0 left-0 bg-green-500">
        </div>
    );
};

export default Header;